spring:
  data:
    mongodb:
#     authentication-database:
#      username: ${MONGO_USERNAME}
#      password: ${MONGO_PASSWORD}
      database: batch
      port: 27017
      host: localhost
  application:
    name: batch-service

